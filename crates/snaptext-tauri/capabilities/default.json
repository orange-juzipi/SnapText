{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for the SnapText shell.",
  "windows": ["main", "overlay", "result"],
  "permissions": ["core:default", "global-shortcut:default"]
}
